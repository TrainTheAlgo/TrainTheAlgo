//const news = require('./news.js');
const writer = require('./writer.js');

const init = async () => {
    const output = writer.write('James Bachini', 'James Bachini is a blockchain developer in the UK')
}

init();